import { Text, View } from "react-native";

export default function Index() {
    return (
        <View className={'flex-1 justify-between items-center'}>
            <Text className={'text-3xl text-sky-500 font-bold'}>Welcome.</Text>
        </View>
    );
}